databaseChangeLog:
  - changeSet:
      id: 001_create_role_table
      author: Yoshua
      comment: "Crea la tabla 'role' base para el control de acceso de usuarios."
      changes:
        - createTable:
            tableName: role
            columns:
              - column:
                  name: role_id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: VARCHAR(100)
                  constraints:
                    unique: true
                    nullable: false
              - column:
                  name: description
                  type: TEXT
                  remarks: "Descripción detallada del rol."
