databaseChangeLog:
  - changeSet:
      id: 005_create_room_table
      author: Yoshua
      comment: "Crea tabla 'room' con columnas VARCHAR para estados y referencias a room_type."
      changes:
        - createTable:
            tableName: room
            columns:
              - column:
                  name: room_id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: room_type_id
                  type: BIGINT
                  constraints:
                    nullable: false
              - column:
                  name: room_number
                  type: VARCHAR(15)
                  constraints:
                    unique: true
                    nullable: false
              - column:
                  name: floor
                  type: INTEGER
              - column:
                  name: current_status
                  type: VARCHAR(20)
                  defaultValue: 'DISPONIBLE'
                  remarks: "Estado actual de la habitación (DISPONIBLE, RESERVADA, OCUPADA, etc.)."
              - column:
                  name: amenities
                  type: VARCHAR(200)
                  constraints:
                    nullable: false
                  remarks: "Comodidades ofrecidas en cada habitación."
              - column:
                  name: nightly_rate
                  type: DECIMAL(10,2)
                  constraints:
                    nullable: false
                  remarks: "Precio por noche de la habitación."
        - addForeignKeyConstraint:
            baseTableName: room
            baseColumnNames: room_type_id
            constraintName: fk_room_room_type
            referencedTableName: room_type
            referencedColumnNames: room_type_id
            onDelete: RESTRICT
